/**
 * Template visibility predicate placeholder.
 * Unit 13 (Templates editor maturity) owns the real implementation.
 * Reserved here so the System Boundaries folder map stays complete.
 */
export const _UNIT_13_PLACEHOLDER = true;
